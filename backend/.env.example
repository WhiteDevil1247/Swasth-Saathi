# Backend envs (Express)
# Copy to .env in this folder for local/dev
PORT=8000
FRONTEND_ORIGIN=http://localhost:3000
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM
# Optional services (not required for demo)
DATABASE_URL=
MONGO_URI=
REDIS_URL=
